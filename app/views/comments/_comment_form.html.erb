<%= form_for [@post, @comment] do |form| %>
  <div class="field mb-3">
    <%= form.label :comment %>
    <%= form.text_area :comment, class: "form-control", placeholder: "Type your comment..." %>
  </div>
  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
